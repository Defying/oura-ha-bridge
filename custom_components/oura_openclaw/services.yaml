refresh:
  name: Refresh
  description: Immediately refresh Oura data for all Oura OpenClaw entries, or one entry ID.
  fields:
    entry_id:
      name: Entry ID
      description: Optional Home Assistant config entry ID to refresh.
      required: false
      selector:
        text:

